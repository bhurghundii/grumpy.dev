{
  "diff": "diff --git a/cache.py b/cache.py\nindex 1111111..2222222 100644\n--- a/cache.py\n+++ b/cache.py\n@@ -10,8 +10,14 @@ class Cache:\n     def get(self, key):\n-        return self._store.get(key)\n+        entry = self._store.get(key)\n+        if entry is None:\n+            return None\n+        value, expires_at = entry\n+        if expires_at < time.time():\n+            del self._store[key]\n+            return None\n+        return value\n \n-    def set(self, key, value):\n-        self._store[key] = value\n+    def set(self, key, value, ttl_seconds=60):\n+        self._store[key] = (value, time.time() + ttl_seconds)\n",
  "question": "What does this change do, and what breaks if it's wrong?",
  "answer": "In get(), self._store.get(key) is now assigned to entry instead of returned directly. There's a check if entry is None, returning None. Then value and expires_at are unpacked from entry. There's a comparison of expires_at to time.time(), and if true, self._store[key] is deleted and None is returned, otherwise value is returned. In set(), the signature gains a ttl_seconds=60 parameter, and self._store[key] is now assigned a tuple of value and time.time() plus ttl_seconds instead of just value.",
  "expected_passed": false,
  "note": "Mechanically restates the diff's code structure line by line -- what got assigned to what, which comparison was added -- without ever using a concept like 'expire', 'stale', 'TTL', or explaining what the change accomplishes or why. This is a diff paste-back in different words, not understanding, and should fail."
}
